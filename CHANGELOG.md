# 更新记录

## v0.2.0 — 2026-08-29

固定 Obsidian 人类入口页与核心卡片展示规范：

- 入口页统一为“定位 → 指标 → 适用场景 → 阅读提醒 → 全部精选 S/A 卡 → 章节地图 → 按问题进入”；
- 核心卡片按 `reading_order` 排序，并用 `display_summary` 提供短说明；
- 增加 Bases 卡片视图与静态 wikilink 回退规则；
- 将统计口径、S/A 选择理由、解析规则、证据边界和重跑说明集中到 `90_AI/<book-slug>/<book-title>｜蒸馏说明.md`；
- 明确章节地图四列结构及 Markdown 表格 wikilink 别名中的 `\|` 转义规则。

## v0.1.1 — 2026-08-29

扩大 PDF 原文导航的上下文范围：

- 重要命中点同时记录核心页与建议回读范围；
- 普通命中点默认前后扩展约 2–4 页，图表、案例、方法和论证转折按完整逻辑单元扩展；
- 范围链接从起始页打开，不转写全文；
- 对图表、无文本页和 OCR 不可靠页面增加人工查看提示；
- 将范围端点和核心页纳入页码校验要求；
- 增加 `source_context_start/end/core_page`、`context_reason` 和 `manual_review` 约定，避免范围过宽、过窄或无法重跑。

## v0.1.0 — 2026-08-27

首个公开版本。

- 建立实用型书籍的 L0–L5 选择性蒸馏流程；
- 增加书籍类型路由、卡片结构、证据边界和导航模板；
- 支持 Obsidian 原始 PDF 页码回溯；
- 明确选中主题、暂缓主题和来源限制；
- 增加开源使用说明与版权边界。
